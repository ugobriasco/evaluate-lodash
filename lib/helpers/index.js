const generateUsers = require("./generate-users");
const {
  generateRandomInt,
  generateArrayOfRandomInt,
} = require("./generate-random-numbers");
const fetchResults = require("./results-fetch");

module.exports = {
  generateUsers,
  generateRandomInt,
  generateArrayOfRandomInt,
  fetchResults,
};
