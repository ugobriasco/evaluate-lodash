const cfg = {
  repetitions: 20, //iteratins of each target operation
  datasetSize: 10000, //size of the data traversed by each target operation
  isCumulative: false, //the results of every test session will cumulate with the data of the past ones
};

module.exports = cfg;
